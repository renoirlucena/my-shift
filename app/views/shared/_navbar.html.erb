<div class="navbar navbar-expand-sm navbar-light navbar-lewagon fixed-top">
  <div class="container-fluid justify-content-between">
    <!-- Button trigger modal -->
    <!-- Button trigger modal -->
    <div>
      <ul class="menuItems">
        <a class="navbar-brand justify-content" href="#">
        <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/logo.png" width="40px" height="40px" /></a>
        <li><a href='#' data-item='Início'>Início</a></li>
        <li><a href='#' data-item='Sobre'>Sobre</a></li>
        <li><a href='#' data-item='Contato'>Contato</a></li>
      </ul>
    </div>
  <div class="container-fluid justify-content-end">
    <%= link_to "New request", new_request_path, class: "btn btn-primary", data: { turbo: true, turbo_frame: "modal"} %>


    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>

        <button type="button" class="btn btn-ghost" data-bs-toggle="modal" data-bs-target="#exampleModal">
          Criar request
        </button>

          <li class="nav-item dropdown">
            <% if current_user.avatar.attached? %>
                <%= cl_image_tag current_user.avatar.key, class: "avatar dropdown-toggle avatar-navbar", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
            <% else %>
              <%= image_tag "https://randomuser.me/api/portraits/women/49.jpg", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
            <% end %>

            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to "Feed", requests_path, class: "dropdown-item" %>
              <%= link_to "Meu perfil", edit_user_registration_path, class: "dropdown-item" %>
              <%= link_to "Sair", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
            </div>
          </li>

        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "btn btn-ghost mx-1" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
